"use strict";

module.exports = {
    index: async function (req, res, next) {
        try {
            await res.send("Hello World")
        } catch(error) {
            next(error)
        }
    },
	// more
};